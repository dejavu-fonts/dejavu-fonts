# $Id$

# script file for FontForge for TTF generation
# usage:
#   mkdir generated
#   fontforge -script generate.pe *.sfd

i = 1
while ( i < $argc )
  Open($argv[i], 1)
  Generate("generated/" + $fontname + ".ttf", "", 0xe0)
  i++
endloop
