language: python
python:
  - "2.7"
# commands to install dependencies
install:
  - sudo apt-get update -qq
  - sudo apt-get install -y fontforge-nox python-fontforge
# commands to run tests
# script:
#   - make
#
after_script:
  - make
